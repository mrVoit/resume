var options = {
    strings: ['Front-End developer', 'Web-designer'],
    typeSpeed: 40,
    backSpeed: 40,
    backDelay: 5000,
    loop: true
  };
  
  var typed = new Typed('.type', options);